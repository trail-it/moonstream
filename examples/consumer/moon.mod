// 最小消费者示例：像外部使用者一样只用公开 API。
//
// 运行（仓库根目录）：moon run --target native examples/consumer
name = "trail-it/moonstream_example"

version = "0.1.0"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ "example", "llm", "streaming" ]

preferred_target = "native"

description = "Minimal consumer example for MoonStream."

import {
  "trail-it/moonstream@0.1.1",
}
